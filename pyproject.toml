[project]
name = "dictation"
version = "0.2.0"
description = "Push-to-talk dictation for Wayland/GNOME"
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
    "sounddevice",
    "soundfile",
    "numpy",
    "groq",
    "deepgram-sdk",
    "PySide6-Essentials",
]

[tool.uv]
package = false
